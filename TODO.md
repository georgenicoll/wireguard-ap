# TODO

- Add a diagnostics page. This should allow us to run various commands
  and see the output - for example, ping to a server, and traceroute to
  a server. The output from the commands can be shown in the output
  modal used for running the management scripts.
