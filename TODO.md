# TODO

- Set up an SSH login banner (`/etc/motd` or similar) on the Pi that explains
  the scripts available in `~` and what each does (`setup_ap.sh`,
  `uplink_wifi.sh`, `view_currently_associated_clients.sh`,
  `setup_forwarding_and_nat.sh`), so a login reminds you what's there instead
  of having to check the README from memory.

- Set up WireGuard routing on the Pi using the peer config generated by
  wireguard-router, so AP clients get routed through that WireGuard endpoint
  and can reach the other peers on that network (see the README intro). Rough
  shape:
  - Grab the client config with `wireguard-router`'s
    `scripts/wg-peer.sh client <client-name>` - probably fetched by `wga`
    itself as part of `apply`, rather than a separate manual step.
  - Use that config to set up WireGuard on the Pi (`wg-quick` or an
    equivalent systemd unit), alongside the existing AP/NAT setup.
  - Once the link is up, route AP client traffic out through the WireGuard
    interface instead of (or as well as) the existing eth0/wlan0 uplink -
    needs thought about how this interacts with `setup_forwarding_and_nat.sh`'s
    NAT rule and the eth0-priority/wlan0-fallback routing in `uplink` mode.
